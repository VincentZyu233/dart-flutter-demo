name: Build and Release

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # Stage 1: Check commit message & extract version
  check-commit:
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.check.outputs.should_build }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check commit message
        id: check
        run: |
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          if echo "$COMMIT_MSG" | grep -qiE "(build action|build release)"; then
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "✓ Commit message contains build trigger"
          else
            echo "should_build=false" >> $GITHUB_OUTPUT
            echo "✗ Commit message does not contain build trigger"
            echo "   Skipping build (commit: ${GITHUB_SHA:0:7})"
          fi

      - name: Extract version from pubspec.yaml
        id: version
        if: steps.check.outputs.should_build == 'true'
        run: |
          FULL_VERSION=$(grep '^version:' pubspec.yaml | head -1 | sed 's/version: //')
          VERSION="${FULL_VERSION%%+*}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Extracted version: $VERSION (from $FULL_VERSION)"

  # Stage 2: Matrix build (Windows x64, Linux x64, Android APK, iOS IPA)
  build:
    needs: check-commit
    if: needs.check-commit.outputs.should_build == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows
            os: windows-latest
            artifact_name: flutter-showcase-windows-x64
            artifact_path: build/windows/x64/runner/Release/
          - platform: linux
            os: ubuntu-latest
            artifact_name: flutter-showcase-linux-x64
            artifact_path: build/linux/x64/release/bundle/
          - platform: android
            os: ubuntu-latest
            artifact_name: flutter-showcase-android
            artifact_path: build/app/outputs/flutter-apk/
          - platform: ios
            os: macos-latest
            artifact_name: flutter-showcase-ios
            artifact_path: build/ios/ipa/
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Generate platform projects
        run: flutter create --project-name flutter_showcase --platforms=windows,linux,android,ios .

      - name: Copy platform plugins
        shell: bash
        run: |
          case "${{ matrix.platform }}" in
            windows)
              mkdir -p windows/runner/plugins/system_info
              cp plugins/windows/system_info_plugin.cpp windows/runner/plugins/system_info/
              cp plugins/windows/system_info_plugin.h windows/runner/plugins/system_info/
              ;;
            linux)
              mkdir -p linux/runner/plugins/system_info
              cp plugins/linux/system_info_plugin.cc linux/runner/plugins/system_info/
              cp plugins/linux/system_info_plugin.h linux/runner/plugins/system_info/
              ;;
            android)
              mkdir -p android/app/src/main/kotlin/com/example/flutter_showcase
              cp plugins/android/SystemInfoPlugin.kt android/app/src/main/kotlin/com/example/flutter_showcase/
              ;;
            ios)
              mkdir -p ios/Runner
              cp plugins/ios/SystemInfoPlugin.swift ios/Runner/
              ;;
          esac
          echo "✓ Copied plugins/${{ matrix.platform }} to platform directory"

      - name: Patch Windows runner for SystemInfo plugin
        if: matrix.platform == 'windows'
        shell: bash
        run: |
          python3 -c "
          # Patch runner/CMakeLists.txt: add plugin source, include, and export flags
          with open('windows/runner/CMakeLists.txt', 'r') as f:
              cmake = f.read()
          cmake = cmake.replace(
              '\"runner/win32_window.cpp\"',
              '\"runner/win32_window.cpp\"\n  \"runner/plugins/system_info/system_info_plugin.cpp\"'
          )
          cmake = cmake.replace(
              'target_include_directories(\${BINARY_NAME} PRIVATE',
              'target_include_directories(\${BINARY_NAME} PRIVATE\n  \"\${CMAKE_CURRENT_SOURCE_DIR}/plugins/system_info\"'
          )
          # Add linker export flags for FFI symbols
          cmake = cmake.replace(
              'target_link_libraries(\${BINARY_NAME} PRIVATE flutter flutter_wrapper_plugin)',
              'target_link_libraries(\${BINARY_NAME} PRIVATE flutter flutter_wrapper_plugin)\n\ntarget_link_options(\${BINARY_NAME} PRIVATE /EXPORT:GetSystemInfoJson /EXPORT:FreeSystemInfoJson)'
          )
          with open('windows/runner/CMakeLists.txt', 'w') as f:
              f.write(cmake)
          "

      - name: Patch Android for SystemInfo plugin
        if: matrix.platform == 'android'
        shell: bash
        run: |
          python3 -c "
          kt_path = 'android/app/src/main/kotlin/com/example/flutter_showcase/MainActivity.kt'
          with open(kt_path, 'r') as f:
              kt = f.read()
          kt = kt.replace(
              'import io.flutter.embedding.android.FlutterActivity',
              'import com.example.flutter_showcase.SystemInfoPlugin\nimport io.flutter.embedding.android.FlutterActivity'
          )
          kt = kt.replace(
              'super.configureFlutterEngine(flutterEngine)',
              'super.configureFlutterEngine(flutterEngine)\n        flutterEngine.plugins.add(SystemInfoPlugin())'
          )
          with open(kt_path, 'w') as f:
              f.write(kt)
          "
          echo "=== Patched MainActivity.kt ===" && cat android/app/src/main/kotlin/com/example/flutter_showcase/MainActivity.kt

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze code
        run: flutter analyze --no-pub || true

      - name: Build (Windows)
        if: matrix.platform == 'windows'
        run: flutter build windows --release

      - name: Build (Linux)
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev
          flutter build linux --release

      - name: Build (Android APK)
        if: matrix.platform == 'android'
        run: flutter build apk --release

      - name: Patch iOS for SystemInfo plugin
        if: matrix.platform == 'ios'
        shell: bash
        run: |
          # Register plugin in AppDelegate.swift
          python3 -c "
          with open('ios/Runner/AppDelegate.swift', 'r') as f:
              swift = f.read()
          swift = swift.replace(
              'GeneratedPluginRegistrant.register(with: self)',
              'GeneratedPluginRegistrant.register(with: self)\n    SystemInfoPlugin.register(with: self)'
          )
          with open('ios/Runner/AppDelegate.swift', 'w') as f:
              f.write(swift)
          "
          # Override code signing via xcconfig (more reliable than sed on pbxproj)
          cat > ios/Flutter/Release.xcconfig << 'XCEOF'
          #include "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
          #include "Generated.xcconfig"
          CODE_SIGN_IDENTITY=
          CODE_SIGNING_REQUIRED=NO
          CODE_SIGNING_ALLOWED=NO
          DEVELOPMENT_TEAM=
          PROVISIONING_PROFILE_SPECIFIER=
          XCEOF
          echo "=== Patched AppDelegate.swift ===" && head -5 ios/Runner/AppDelegate.swift

      - name: Build (iOS IPA - no codesign)
        if: matrix.platform == 'ios'
        run: |
          # Build Flutter app without codesign
          flutter build ios --release --no-codesign
          # Package IPA manually
          mkdir -p build/ios/ipa
          cd build/ios/iphoneos
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r ../ipa/flutter-showcase-ios.ipa Payload
          cd ../..

      - name: Upload Windows artifact
        if: matrix.platform == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: build/windows/x64/runner/Release/
          retention-days: 7

      - name: Upload Linux artifact
        if: matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: build/linux/x64/release/bundle/
          retention-days: 7

      - name: Upload Android artifact
        if: matrix.platform == 'android'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 7

      - name: Upload iOS artifact
        if: matrix.platform == 'ios'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: build/ios/ipa/flutter-showcase-ios.ipa
          retention-days: 7

  # Stage 3: Package & publish release
  publish:
    needs: [check-commit, build]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Package artifacts with version
        run: |
          VERSION="${{ needs.check-commit.outputs.version }}"

          # Windows: zip the directory
          cd artifacts/flutter-showcase-windows-x64
          zip -r "../flutter-showcase-windows-x64-v${VERSION}.zip" .
          cd ../..

          # Linux: tar.gz the directory
          cd artifacts/flutter-showcase-linux-x64
          tar -czf "../flutter-showcase-linux-x64-v${VERSION}.tar.gz" .
          cd ../..

          # Android: rename the APK
          cp artifacts/flutter-showcase-android/app-release.apk \
             "artifacts/flutter-showcase-android-v${VERSION}.apk"

          # iOS: rename the IPA
          cp artifacts/flutter-showcase-ios/flutter-showcase-ios.ipa \
             "artifacts/flutter-showcase-ios-v${VERSION}.ipa"

          echo "📦 Packaged artifacts:"
          find artifacts/ -maxdepth 1 -type f

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.check-commit.outputs.version }}
          name: Flutter Showcase v${{ needs.check-commit.outputs.version }}
          body: |
            ## Flutter Showcase Build

            **Version:** `${{ needs.check-commit.outputs.version }}`

            ### Downloads

            | Platform | File |
            |----------|------|
            | Windows x64 | `flutter-showcase-windows-x64-v${{ needs.check-commit.outputs.version }}.zip` |
            | Linux x64 | `flutter-showcase-linux-x64-v${{ needs.check-commit.outputs.version }}.tar.gz` |
            | Android APK | `flutter-showcase-android-v${{ needs.check-commit.outputs.version }}.apk` |
            | iOS IPA | `flutter-showcase-ios-v${{ needs.check-commit.outputs.version }}.ipa` |

            ### Build Info
            - Triggered by: `${{ github.event.head_commit.message }}`
            - Commit: `${{ github.sha }}`
          files: |
            artifacts/flutter-showcase-windows-x64-v*.zip
            artifacts/flutter-showcase-linux-x64-v*.tar.gz
            artifacts/flutter-showcase-android-v*.apk
            artifacts/flutter-showcase-ios-v*.ipa
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Success notification
        run: |
          echo "✅ Build and release completed!"
          echo "   Version: ${{ needs.check-commit.outputs.version }}"
          echo "   Release: https://github.com/${{ github.repository }}/releases/tag/v${{ needs.check-commit.outputs.version }}"
